const IPv4Address = require('./lib/IPv4Address');
const IPv4Network = require('./lib/IPv4Network');

/*
  You do not need to do anything in this file.
  You need to implement the imported classes above,
  each in its own file, in the lib folder.
 */

module.exports = {
  IPv4Address,
  IPv4Network
};
