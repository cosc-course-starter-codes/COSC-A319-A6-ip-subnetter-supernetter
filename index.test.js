require('jest');

const { IPv4Address, IPv4Network } = require('./index');

describe('IPv4Address', () => {
  xit('needs tests', () => { });
});

describe('IPv4Network', () => {
  xit('needs tests', () => { });
});
